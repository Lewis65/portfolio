import styled from 'styled-components'
import React from 'react'

const CardWrapper = styled.div`

`

const Card = (props) => {
    return <CardWrapper>
        
    </CardWrapper>
}

export default Card